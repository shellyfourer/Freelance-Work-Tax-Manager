services:
  backend:
    build:
      context: ./backend
    image: tax-manager-backend
    ports:
      - "8080:8080"

  frontend:
    build:
      context: ./frontend
    image: tax-manager-frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend