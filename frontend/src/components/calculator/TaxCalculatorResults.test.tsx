describe("TaxCalculatorResults", () => {
  test.todo("renders placeholder state when result is null");
  test.todo("renders loading state when isCalculating is true");
  test.todo("renders gross income, net income, and total tax when result is provided");
  test.todo("renders each line item when lineItems is non-empty");
  test.todo("renders no line items section when lineItems is empty");
});
