describe("calculateTax", () => {
  test.todo("calls POST /api/tax/calculate with the correct request body");
  test.todo("returns mapped TaxCalculatorResult on a successful response");
  test.todo("throws on a non-ok response");
});
